<?xml version="1.0"?>
<psalm
    errorLevel="1"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config ../vendor/vimeo/psalm/config.xsd"
    findUnusedBaselineEntry="true"
    findUnusedCode="true"
>
    <projectFiles>
        <directory name="../src/" />
        <ignoreFiles>
            <directory name="../vendor" />
        </ignoreFiles>
    </projectFiles>
    <issueHandlers>
        <!-- MissingOverrideAttribute requires PHP 8.3+, but we support 8.2+ -->
        <MissingOverrideAttribute errorLevel="suppress" />
        <!-- MissingClassConstType requires PHP 8.3+ typed constants, but we support 8.2+ -->
        <MissingClassConstType errorLevel="suppress" />
        <!-- Public API classes intended for library consumers -->
        <ClassMustBeFinal errorLevel="suppress" />
        <!-- Public API methods/classes intended for library consumers -->
        <PossiblyUnusedMethod errorLevel="suppress" />
        <PossiblyUnusedProperty errorLevel="suppress" />
        <UnusedClass errorLevel="suppress" />
        <UnusedProperty errorLevel="suppress" />
    </issueHandlers>
</psalm>
